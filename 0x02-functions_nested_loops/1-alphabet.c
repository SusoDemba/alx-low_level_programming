#include "main.h"

/**
 * print_alphabet - prints the letters of the alphabet
 * in lower case
 * Return: nothing (void)
 */
void print_alphabet(void)
{
	char ch;
	for (a ='a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
